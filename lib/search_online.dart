import 'package:flutter/material.dart';

class SearchOnline extends StatelessWidget {
  const SearchOnline({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Search from Book API'),
      ),
    );
  }
}